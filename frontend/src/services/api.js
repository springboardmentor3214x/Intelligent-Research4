export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000'
export async function apiFetch(path, options = {}) { const response = await fetch(`${API_BASE_URL}${path}`, options); if (!response.ok) { let detail = 'Something went wrong. Please try again.'; try { const body = await response.json(); detail = typeof body.detail === 'string' ? body.detail : Array.isArray(body.detail) ? body.detail[0]?.msg || detail : detail } catch { /* non-JSON error */ } throw new Error(detail) } return response.status === 204 ? null : response.json() }
